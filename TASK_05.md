# TASK 5 — Records / top scorers (parked; substrate confirmed ready, one method missing)

> Conventions and commands: see the backlog index (Claude plan file). Run everything via `mise exec -- pnpm <cmd>`, never commit.

**Confirmed**: `KEY_LOG_EVENTS` (`packages/engine/src/competition/competition-manager.ts:34-36`) = `goal, yellow_card, red_card, match.substitution_applied, match.completed, injury` — exactly the pruned set, wired via `new EventLog({keep: e => KEY_LOG_EVENTS.has(e.eventType)})` in `newEngine()` (line ~100-109). Confirmed NOT persisted anywhere (`grep -n "EventLog\|CompetitionManager" backend/src/data/save-data.ts` → zero matches) — a mid-season save/load loses it; competitions rebuild from scratch on every load and every season rollover, so the log resets "for free" as intended.

**Gap that must be closed before this feature can be built**: `CompetitionManager` has NO public method that reads its `EventLog`'s contents (full method list checked — nothing exposes it). `EventLog.query(filter)` itself IS public (`packages/timeline/src/event-log.ts:39`), but `TickEngine.eventLog` is `private readonly` (`packages/timeline/src/tick-engine.ts:27`) with no getter anywhere — so today there is genuinely no way to read the retained events from outside, at any layer. First implementation step: add either `CompetitionManager.getEventLog(): readonly OccurrenceEvent[]` (needs `TickEngine` to expose its private `eventLog` first — add a getter there) or a purpose-built query like `CompetitionManager.topScorers(): {playerId, goals}[]` that reaches through internally. The latter is probably cleaner (keeps `EventLog` an implementation detail).
